# 🔄 Обновление контракта - Публичный минт

## ✅ Что изменено

**Проблема:** Транзакция падала с ошибкой "execution reverted" потому что функция `mintTo` имела модификатор `onlyOwner`, и только владелец контракта мог минтить NFT.

**Решение:** Убран модификатор `onlyOwner` из функции `mintTo`, теперь любой пользователь может минтить NFT для себя.

## 📍 Новый адрес контракта

**Адрес:** `0x2D2cDdB90411D9adb3507C3cd030587002128985`

**Сеть:** Base Sepolia (Testnet)

**Старый адрес (устарел):** `0xfADA27f54C48406BdAfc4c6084d7aDA4aafC9220`

## 🔧 Что нужно обновить

### 1. Локально (.env.local)

Уже обновлено автоматически:
```env
NEXT_PUBLIC_CONTRACT_ADDRESS=0x2D2cDdB90411D9adb3507C3cd030587002128985
```

### 2. В Vercel (ОБЯЗАТЕЛЬНО!)

1. Перейдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Settings → Environment Variables
4. Найдите `NEXT_PUBLIC_CONTRACT_ADDRESS`
5. Обновите значение на: `0x2D2cDdB90411D9adb3507C3cd030587002128985`
6. Сохраните
7. **Передеплойте проект** (Deployments → ... → Redeploy)

### 3. Проверка на BaseScan

Новый контракт: https://sepolia.basescan.org/address/0x2D2cDdB90411D9adb3507C3cd030587002128985

## 🎯 Изменения в контракте

### До:
```solidity
function mintTo(address to, string calldata tokenURI) external onlyOwner returns (uint256)
```

### После:
```solidity
function mintTo(address to, string calldata tokenURI) external returns (uint256)
```

Теперь любой пользователь может вызвать `mintTo` и заминтить NFT для себя.

## ✅ Тестирование

После обновления адреса в Vercel и передеплоя:
- Любой пользователь сможет минтить NFT при создании поста
- Транзакции не будут падать с ошибкой "execution reverted"
- Каждый пользователь получит свой NFT

## 📋 Чеклист

- [x] Контракт обновлен и задеплоен
- [x] `.env.local` обновлен
- [ ] Vercel Environment Variables обновлены
- [ ] Проект передеплоен в Vercel
- [ ] Протестирован минт в приложении

