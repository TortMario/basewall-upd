# 🔄 Обновление системы - Жесткая система NFT

## ✅ Что изменено

### 1. Контракт обновлен

**Новые функции:**
- ✅ Плата за минт: **0.05 USD** (0.0001 ETH) - идет владельцу контракта
- ✅ Роялти: **3%** со всех продаж NFT (ERC2981 стандарт)
- ✅ Сжигание NFT при удалении поста
- ✅ Публичный минт (любой может минтить)

**Адрес нового контракта:** Будет задеплоен после компиляции

### 2. Логика приложения

**Создание постов:**
- ✅ Посты **НЕ появляются** без подтвержденного NFT
- ✅ Пост создается только после успешного минта
- ✅ Если транзакция отменена - пост не создается

**Удаление постов:**
- ✅ При удалении поста NFT **сжигается** (burn)
- ✅ Сначала сжигается NFT, потом удаляется пост
- ✅ Только владелец NFT может удалить пост

**Права на пост:**
- ✅ При передаче NFT передается право на пост
- ✅ Владелец NFT может редактировать и удалять пост
- ✅ Проверка владельца происходит on-chain

### 3. Монетизация

**Доходы владельца контракта:**
- 💰 **0.05 USD** за каждый минт NFT
- 💰 **3% роялти** со всех продаж NFT на маркетплейсах

## 📋 Что нужно сделать

### 1. Задеплоить новый контракт

```bash
HARDHAT_NETWORK=baseSepolia npm run deploy:sepolia
```

### 2. Обновить адрес контракта

После деплоя обновить:
- `.env.local`: `NEXT_PUBLIC_CONTRACT_ADDRESS=новый_адрес`
- Vercel Environment Variables: `NEXT_PUBLIC_CONTRACT_ADDRESS=новый_адрес`

### 3. Очистить все существующие посты

```bash
npm run clear:posts
```

Это удалит все посты, которые были созданы без NFT или с неуспешным минтом.

### 4. Передеплой приложения

После обновления адреса контракта в Vercel:
- Передеплойте проект
- Проверьте работу минта

## 🎯 Новый флоу создания поста

1. Пользователь вводит текст поста
2. Нажимает "Post"
3. Создается запись в БД со статусом `pending`
4. Пользователь подтверждает транзакцию минта (с платой 0.0001 ETH)
5. После подтверждения транзакции:
   - Обновляется статус поста на `success`
   - Сохраняется `tokenId`
   - Пост появляется в ленте
6. Если транзакция отменена - пост остается в статусе `pending` и **не показывается**

## 🔥 Флоу удаления поста

1. Пользователь нажимает "Delete"
2. Подтверждает удаление
3. Вызывается функция `burn(tokenId)` в контракте
4. После подтверждения сжигания NFT:
   - Пост удаляется из БД
   - Пост исчезает из ленты

## 💰 Монетизация

### Доходы от минта

Каждый минт приносит **0.05 USD** (0.0001 ETH) владельцу контракта.

Средства автоматически отправляются на адрес владельца при минте.

### Доходы от роялти

При продаже NFT на маркетплейсах (OpenSea, Blur и т.д.):
- Автоматически **3%** от суммы продажи идет владельцу контракта
- Работает через стандарт ERC2981

## 📊 Проверка

После деплоя проверьте:

1. ✅ Минт работает с платой 0.0001 ETH
2. ✅ Посты появляются только после успешного минта
3. ✅ Удаление поста сжигает NFT
4. ✅ Передача NFT передает права на пост
5. ✅ Роялти настроены (3%)

## 🚀 Готово!

Теперь система работает по принципу: **Нет NFT - нет поста!**

