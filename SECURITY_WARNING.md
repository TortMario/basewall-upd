# ⚠️ КРИТИЧЕСКОЕ ПРЕДУПРЕЖДЕНИЕ О БЕЗОПАСНОСТИ

## ❌ НЕ ХРАНИТЕ ПРИВАТНЫЙ КЛЮЧ НА VERCEL!

**Приватный ключ (`PRIVATE_KEY`) НЕ должен быть в переменных окружения Vercel!**

### Почему это опасно?

1. **Приватный ключ дает полный доступ к вашему кошельку**
   - Любой, кто получит доступ к переменным окружения Vercel, сможет украсть все средства
   - Приватный ключ можно использовать для подписания транзакций от вашего имени

2. **Vercel переменные окружения могут быть скомпрометированы**
   - Логи могут содержать значения переменных
   - Сотрудники Vercel могут иметь доступ (в крайних случаях)
   - Утечки через GitHub/другие сервисы

### ✅ Правильное использование приватного ключа

**Приватный ключ нужен ТОЛЬКО для:**
- Локальной разработки (`.env.local` - НЕ коммитится в Git)
- Деплоя контрактов через Hardhat (локально или на CI/CD с защищенными секретами)

**Приватный ключ НЕ нужен для:**
- Работы приложения на Vercel
- Фронтенд кода
- API роутов (если они не деплоят контракты)

### 🔧 Что нужно сделать СЕЙЧАС:

1. **Удалите `PRIVATE_KEY` из Vercel Environment Variables:**
   - Зайдите в Vercel Dashboard
   - Выберите ваш проект
   - Settings → Environment Variables
   - Найдите `PRIVATE_KEY` и **УДАЛИТЕ** его

2. **Проверьте, что приватный ключ не попал в Git:**
   ```bash
   git log --all --full-history --source -- "*PRIVATE_KEY*"
   ```
   
   Если нашли - **немедленно смените приватный ключ** и переместите средства на новый кошелек!

3. **Используйте приватный ключ только локально:**
   - Храните в `.env.local` (уже в `.gitignore`)
   - Используйте только для деплоя контрактов через Hardhat

### 📋 Переменные, которые ДОЛЖНЫ быть на Vercel:

✅ `NEXT_PUBLIC_CONTRACT_ADDRESS` - публичный адрес контракта (безопасно)
✅ `NEXT_PUBLIC_BASE_RPC_URL` - публичный RPC URL (безопасно)
✅ `KV_REST_API_URL` - URL для базы данных (безопасно)
✅ `KV_REST_API_TOKEN` - токен для базы данных (безопасно)
✅ `ETHERSCAN_API_KEY` - API ключ для верификации (опционально, безопасно)

### ❌ Переменные, которые НЕ должны быть на Vercel:

❌ `PRIVATE_KEY` - **КРИТИЧЕСКАЯ ОШИБКА БЕЗОПАСНОСТИ!**
❌ `MNEMONIC` - seed фраза (если используется)
❌ Любые другие приватные ключи или seed фразы

### 🛡️ Если приватный ключ был скомпрометирован:

1. **Немедленно создайте новый кошелек**
2. **Переместите все средства** на новый кошелек
3. **Обновите `PRIVATE_KEY` в `.env.local`** с новым ключом
4. **Задеплойте контракт заново** с новым кошельком (или передайте ownership)

---

**Помните: Приватный ключ = Полный доступ к кошельку. Никогда не храните его в публичных местах!**

