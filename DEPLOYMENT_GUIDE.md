# 🚀 Полное руководство по деплою контракта на Base

## 📋 Содержание

1. [Подготовка](#подготовка)
2. [Настройка окружения](#настройка-окружения)
3. [Деплой на Base Sepolia](#деплой-на-base-sepolia)
4. [Тестирование](#тестирование)
5. [Деплой на Base Mainnet](#деплой-на-base-mainnet)
6. [Настройка Vercel](#настройка-vercel)

---

## 🔧 Подготовка

### Шаг 1: Проверка зависимостей

Убедитесь, что все зависимости установлены:

```bash
npm install
```

### Шаг 2: Компиляция контракта

Проверьте, что контракт компилируется без ошибок:

```bash
npm run compile
```

Если есть ошибки, исправьте их перед деплоем.

---

## ⚙️ Настройка окружения

### Шаг 1: Создайте `.env.local`

Скопируйте пример файла:

```bash
cp .env.local.example .env.local
```

### Шаг 2: Заполните необходимые переменные

Откройте `.env.local` и заполните:

#### 🔑 PRIVATE_KEY (обязательно)

**⚠️ ВАЖНО: Никогда не коммитьте этот файл в Git!**

1. Откройте MetaMask (или другой кошелек)
2. Выберите аккаунт для деплоя
3. Нажмите на три точки → Account Details → Show Private Key
4. Скопируйте приватный ключ (формат: `0x...`)
5. Вставьте в `.env.local`:

```env
PRIVATE_KEY=0xваш_приватный_ключ_здесь
```

#### 🌐 NEXT_PUBLIC_BASE_RPC_URL

Для Base Sepolia (тестовая сеть):
```env
NEXT_PUBLIC_BASE_RPC_URL=https://sepolia.base.org
```

Для Base Mainnet (основная сеть):
```env
NEXT_PUBLIC_BASE_RPC_URL=https://mainnet.base.org
```

#### 🔍 ETHERSCAN_API_KEY (опционально)

Для верификации контракта на BaseScan:

1. Зарегистрируйтесь на [basescan.org](https://basescan.org)
2. Перейдите в [My API Key](https://basescan.org/myapikey)
3. Создайте API ключ
4. Добавьте в `.env.local`:

```env
ETHERSCAN_API_KEY=ваш_api_ключ
```

#### 📦 BASE_URI (опционально)

URI для метаданных NFT. Можно использовать:
- IPFS: `ipfs://QmYourHash/`
- Ваш API: `https://your-api.com/metadata/`

```env
BASE_URI=ipfs://QmYourIPFSHash/
```

---

## 🧪 Деплой на Base Sepolia

### Шаг 1: Получите тестовые ETH

1. Перейдите на [Base Sepolia Faucet](https://www.coinbase.com/faucets/base-ethereum-goerli-faucet)
2. Введите адрес вашего кошелька
3. Получите тестовые ETH (обычно 0.1-0.5 ETH)

### Шаг 2: Проверьте баланс

Убедитесь, что у вас есть ETH на кошельке для оплаты газа.

### Шаг 3: Деплой контракта

```bash
npm run deploy:sepolia
```

Или:

```bash
npx hardhat run scripts/deploy.ts --network baseSepolia
```

### Шаг 4: Сохраните адрес контракта

После успешного деплоя вы увидите:

```
✅ Deployment Successful!
📍 Contract Address: 0x...
```

**Скопируйте этот адрес!** Он понадобится для:
- Тестирования
- Настройки Vercel
- Деплоя на Mainnet

### Шаг 5: Обновите `.env.local`

Добавьте адрес контракта:

```env
NEXT_PUBLIC_CONTRACT_ADDRESS=0xваш_адрес_контракта
```

---

## 🧪 Тестирование

### Тестовый минт NFT

После деплоя протестируйте минт:

```bash
npm run mint:test -- --network baseSepolia 0xваш_адрес_контракта
```

Или если адрес уже в `.env.local`:

```bash
npm run mint:test -- --network baseSepolia
```

### Проверка на BaseScan

1. Откройте [sepolia.basescan.org](https://sepolia.basescan.org)
2. Вставьте адрес контракта в поиск
3. Проверьте:
   - ✅ Контракт задеплоен
   - ✅ Транзакции видны
   - ✅ Можно вызвать функции (если верифицирован)

### Верификация контракта (опционально)

```bash
npx hardhat verify --network baseSepolia 0xваш_адрес_контракта "0xадрес_владельца" "ipfs://QmYourHash/"
```

---

## 🌐 Деплой на Base Mainnet

### ⚠️ ВАЖНО: Перед деплоем на Mainnet

1. ✅ Протестируйте на Sepolia
2. ✅ Убедитесь, что контракт работает правильно
3. ✅ Проверьте, что у вас достаточно ETH для газа
4. ✅ Сохраните адрес контракта из Sepolia (для справки)

### Шаг 1: Обновите `.env.local`

Измените RPC URL на Mainnet:

```env
NEXT_PUBLIC_BASE_RPC_URL=https://mainnet.base.org
```

### Шаг 2: Деплой на Mainnet

```bash
npm run deploy:mainnet
```

Или:

```bash
npx hardhat run scripts/deploy.ts --network base
```

### Шаг 3: Сохраните адрес Mainnet контракта

Скопируйте новый адрес контракта.

### Шаг 4: Обновите `.env.local`

```env
NEXT_PUBLIC_CONTRACT_ADDRESS=0xваш_адрес_контракта_mainnet
```

---

## 🚀 Настройка Vercel

### Шаг 1: Добавьте переменные окружения

1. Перейдите в [Vercel Dashboard](https://vercel.com/dashboard)
2. Выберите ваш проект
3. Settings → Environment Variables
4. Добавьте:

| Key | Value | Environment |
|-----|-------|-------------|
| `NEXT_PUBLIC_CONTRACT_ADDRESS` | `0xваш_адрес_контракта` | Production, Preview, Development |
| `NEXT_PUBLIC_BASE_RPC_URL` | `https://mainnet.base.org` | Production |
| `NEXT_PUBLIC_BASE_RPC_URL` | `https://sepolia.base.org` | Preview, Development |

### Шаг 2: Передеплойте проект

1. Перейдите в Deployments
2. Нажмите на последний деплой
3. Нажмите "..." → Redeploy

Или сделайте новый коммит и пуш:

```bash
git add .
git commit -m "Update contract address"
git push origin main
```

---

## 📝 Чеклист деплоя

### Base Sepolia
- [ ] Установлены зависимости (`npm install`)
- [ ] Создан `.env.local` с `PRIVATE_KEY`
- [ ] Получены тестовые ETH на Base Sepolia
- [ ] Контракт скомпилирован (`npm run compile`)
- [ ] Контракт задеплоен (`npm run deploy:sepolia`)
- [ ] Адрес контракта сохранен
- [ ] Протестирован минт (`npm run mint:test`)
- [ ] Контракт проверен на BaseScan

### Base Mainnet
- [ ] Все тесты на Sepolia пройдены
- [ ] Обновлен `NEXT_PUBLIC_BASE_RPC_URL` на Mainnet
- [ ] Есть достаточно ETH для газа
- [ ] Контракт задеплоен на Mainnet (`npm run deploy:mainnet`)
- [ ] Адрес Mainnet контракта сохранен
- [ ] Обновлены переменные в Vercel
- [ ] Проект передеплоен в Vercel

---

## 🆘 Решение проблем

### Ошибка: "Insufficient funds"

**Проблема:** Недостаточно ETH для оплаты газа.

**Решение:**
- Для Sepolia: получите тестовые ETH из faucet
- Для Mainnet: пополните кошелек реальными ETH

### Ошибка: "Contract deployment failed"

**Проблема:** Ошибка при деплое.

**Решение:**
1. Проверьте, что `PRIVATE_KEY` правильный
2. Проверьте баланс кошелька
3. Проверьте RPC URL
4. Попробуйте увеличить gas price в `hardhat.config.ts`

### Ошибка: "Cannot read property 'address' of undefined"

**Проблема:** Контракт не найден.

**Решение:**
1. Убедитесь, что контракт задеплоен
2. Проверьте адрес контракта в `.env.local`
3. Проверьте, что используете правильную сеть

---

## 📚 Полезные ссылки

- [Base Sepolia Faucet](https://www.coinbase.com/faucets/base-ethereum-goerli-faucet)
- [BaseScan Sepolia](https://sepolia.basescan.org)
- [BaseScan Mainnet](https://basescan.org)
- [Base Documentation](https://docs.base.org)
- [Hardhat Documentation](https://hardhat.org/docs)

---

## ✅ Готово!

После выполнения всех шагов у вас будет:
- ✅ Задеплоенный контракт на Base Sepolia
- ✅ Задеплоенный контракт на Base Mainnet
- ✅ Настроенные переменные окружения в Vercel
- ✅ Рабочее приложение с подключенным контрактом

Удачи! 🎉

