# Настройка прав администратора

## Как стать админом в своем мини-приложении

Для того чтобы получить права администратора в вашем мини-приложении, нужно настроить переменные окружения в Vercel.

### Шаги настройки:

1. **Откройте ваш проект в Vercel**
   - Перейдите на [vercel.com](https://vercel.com)
   - Выберите ваш проект

2. **Перейдите в настройки переменных окружения**
   - Settings → Environment Variables

3. **Добавьте следующие переменные:**

   ```
   ADMIN_USERNAME=ваш_username_в_farcaster
   ADMIN_ADDRESS=ваш_адрес_кошелька_base
   ADMIN_FID=ваш_fid_номер
   NEXT_PUBLIC_ADMIN_USERNAME=ваш_username_в_farcaster
   NEXT_PUBLIC_ADMIN_ADDRESS=ваш_адрес_кошелька_base
   NEXT_PUBLIC_ADMIN_FID=ваш_fid_номер
   ```
   
   **ВАЖНО:** FID - это самый надежный способ проверки админа, так как username может не получаться через SDK. Рекомендуется обязательно указать FID.

   **Пример:**
   ```
 
   ```

4. **Установите переменные для всех окружений:**
   - ✅ Production
   - ✅ Preview
   - ✅ Development

5. **Перезапустите деплоймент**
   - После добавления переменных, Vercel автоматически перезапустит деплоймент
   - Или вы можете вручную перезапустить: Deployments → ... → Redeploy

### Как узнать свой username, FID и адрес:

1. **Farcaster FID (ВАЖНО! Самый надежный способ):**
   - Откройте Base App
   - Ваш FID можно найти в URL вашего профиля: `https://base.app/profile/12345` (где 12345 - это ваш FID)
   - Или откройте консоль браузера в Base App и выполните: `await sdk.context` - там будет `user.fid`
   - FID - это число (например, `12345`)

2. **Farcaster Username:**
   - Откройте Base App
   - Ваш username отображается в профиле (например, `@mynameisthe`)
   - Или проверьте на [base.app/profile/ваш_username](https://base.app)

3. **Base Wallet Address:**
   - Откройте Base App
   - Перейдите в настройки кошелька
   - Скопируйте ваш Base адрес (начинается с `0x`)

### Проверка прав администратора:

После настройки переменных и перезапуска деплоймента, вы должны увидеть:

- ⭐ Кнопку для выделения постов (highlight)
- 👁️ Кнопку для скрытия постов (hide)
- 🗑️ Возможность удалять любые посты (не только свои)

### Важные замечания:

1. **Переменные должны совпадать:**
   - `ADMIN_USERNAME` и `NEXT_PUBLIC_ADMIN_USERNAME` должны иметь одинаковое значение
   - `ADMIN_ADDRESS` и `NEXT_PUBLIC_ADMIN_ADDRESS` должны иметь одинаковое значение

2. **Проверка админа работает по (в порядке приоритета):**
   - **FID** (самый надежный способ - рекомендуется обязательно указать)
   - Username (ваш Farcaster username)
   - Адрес кошелька (ваш Base wallet address)

3. **После изменения переменных:**
   - Обязательно перезапустите деплоймент
   - Подождите завершения деплоймента
   - Очистите кеш браузера, если изменения не применяются

### Устранение проблем:

**Проблема:** Не вижу кнопки админа
- ✅ Проверьте, что переменные окружения установлены в Vercel
- ✅ Убедитесь, что вы используете правильный username (точно такой же, как в Base App)
- ✅ Проверьте, что адрес кошелька правильный (с учетом регистра, начинается с `0x`)
- ✅ Перезапустите деплоймент после изменения переменных
- ✅ Очистите кеш браузера

**Проблема:** Получаю ошибку 403 при попытке выделить/скрыть пост
- ✅ Проверьте логи в Vercel (Functions → Logs)
- ✅ Убедитесь, что username передается правильно (может быть проблема с регистром)
- ✅ Проверьте, что адрес кошелька совпадает (с учетом регистра)

### Локальная разработка:

Для локальной разработки создайте файл `.env.local`:

```env
ADMIN_USERNAME=ваш_username
ADMIN_ADDRESS=ваш_адрес
NEXT_PUBLIC_ADMIN_USERNAME=ваш_username
NEXT_PUBLIC_ADMIN_ADDRESS=ваш_адрес
```

Затем перезапустите dev сервер:
```bash
npm run dev
```

