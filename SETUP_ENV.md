# ⚙️ Настройка .env.local

## ⚠️ ВАЖНО: Безопасность

**НИКОГДА не делитесь seed фразой или приватным ключом!**
- Seed фраза используется для восстановления всего кошелька
- Приватный ключ нужен для деплоя контракта
- Оба должны храниться в секрете!

## 🔑 Как получить приватный ключ

### Из MetaMask:

1. Откройте MetaMask
2. Нажмите на три точки (⋮) → **Account Details**
3. Нажмите **Show Private Key**
4. Введите пароль MetaMask
5. Скопируйте приватный ключ (формат: `0x...`)

### Из Coinbase Wallet:

1. Откройте Coinbase Wallet
2. Настройки (⚙️) → **Show Private Key**
3. Подтвердите паролем/биометрией
4. Скопируйте приватный ключ

## 📝 Создание .env.local

Создайте файл `.env.local` в корне проекта:

```env
# ============================================
# Hardhat Configuration
# ============================================

# Приватный ключ кошелька (НЕ seed фраза!)
# Получите его из MetaMask или Coinbase Wallet
# Формат: 0x... (64 hex символа)
PRIVATE_KEY=0xваш_приватный_ключ_здесь

# Base RPC URL
# Для Sepolia (тестовая сеть):
NEXT_PUBLIC_BASE_RPC_URL=https://sepolia.base.org

# Для Mainnet (основная сеть):
# NEXT_PUBLIC_BASE_RPC_URL=https://mainnet.base.org

# BaseScan API Key для верификации контракта
# Получите на: https://basescan.org/myapikey
ETHERSCAN_API_KEY=2HWTT8UQ9K6KZ2RIFK8FVHA4Q83CRDN8VG

# Base URI для NFT metadata (опционально)
# Можно использовать IPFS или ваш API
BASE_URI=ipfs://QmYourIPFSHash/

# ============================================
# Next.js / Application Configuration
# ============================================

# Адрес контракта (будет установлен после деплоя)
NEXT_PUBLIC_CONTRACT_ADDRESS=0x0000000000000000000000000000000000000000

# Mini App URL
NEXT_PUBLIC_MINIAPP_URL=https://your-app.vercel.app
NEXT_PUBLIC_MINIAPP_NAME=OneStream

# Database (Vercel KV - автоматически настроено в Vercel)
KV_REST_API_URL=
KV_REST_API_TOKEN=
```

## ✅ Проверка

Убедитесь, что:
- ✅ `.env.local` существует
- ✅ `PRIVATE_KEY` начинается с `0x` и имеет 66 символов (0x + 64 hex)
- ✅ `ETHERSCAN_API_KEY` заполнен
- ✅ `.env.local` в `.gitignore` (уже настроено)

## 🚀 Следующие шаги

После настройки `.env.local`:

1. Получите тестовые ETH на Base Sepolia:
   - https://www.coinbase.com/faucets/base-ethereum-goerli-faucet

2. Деплой контракта:
   ```bash
   npm run compile
   npm run deploy:sepolia
   ```

3. Сохраните адрес контракта в `.env.local`:
   ```env
   NEXT_PUBLIC_CONTRACT_ADDRESS=0xваш_адрес_контракта
   ```

